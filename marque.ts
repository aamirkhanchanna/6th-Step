function marqueStudents(){
    const totalStudents:number = 700;
    const compStudent:number = 100;
    const laptop_Students:number = totalStudents - compStudent;
    return laptop_Students;
}

// console.log(marqueStudents())
export default marqueStudents